# BwalyaElijah178

Zambia Universities Sports Tournament — Landing Page

Files added:

- `index.html` — main landing page
- `styles.css` — styling and responsive layout
- `script.js` — mobile nav toggle and demo form handler

To view locally, open `index.html` in your browser or run a simple static server, for example:

Windows PowerShell:

```powershell
Start-Process index.html
```

Or using Python HTTP server:

```bash
python -m http.server 8000
# then open http://localhost:8000
```

If you'd like, I can add images, a real schedule JSON, or wire up a backend form endpoint next.